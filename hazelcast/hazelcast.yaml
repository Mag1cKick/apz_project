hazelcast:
  cluster-name: robotops-hz
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        members:
          - hazelcast1
          - hazelcast2
          - hazelcast3
    interfaces:
      enabled: false
